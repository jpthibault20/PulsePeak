generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["reactNative"]
}

datasource db {
  provider = "postgresql"
  url      = env("SUPABASE_TABLE_URL")
}

// Your data model

model User {
  id         Int      @id @default(autoincrement())
  created_at DateTime @default(now())
  session_id String   @unique
  lastName   String
  firstName  String
}
