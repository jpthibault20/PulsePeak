import React, { useState } from 'react'
import { View, Text } from 'react-native'

export const WeeklyView = ({ calendar }) => {
    return (
        <View className="w-full items-center">
            <Text className="font-mbold text-xl">
                Weekly view
            </Text>
        </View>
    )
}